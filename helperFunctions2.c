#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
  * writedecimal - prints decimal numbers
  * @n: input num
  *
  * Return: num length
  */

int writedecimal(int n)
{
	int i;
	char *s;

	for (i = 0; i < n; i++)
		s[i] = n;
	write(1, s, 1);
	return (i);
}
